#include <stdio.h>

int main()
{
    // int i = 34;
    // int *ptr = &i;
    // printf("THE ADDRESS OF i IS %u\n", ptr);
    // ptr++;
    // ptr++;
    // ptr++;
    // ptr--;
    // ptr=ptr+1;
    // ptr = ptr - 1;
    // printf("THE ADDRESS OF i IS %u\n", ptr);

    // char i = 34;
    // char *ptr = &i;
    // printf("THE ADDRESS OF i IS %u\n", ptr);
    // ptr++;
    // ptr++;
    // ptr++;
    // ptr--;
    // ptr=ptr+1;
    // ptr = ptr - 1;
    // printf("THE ADDRESS OF i IS %u\n", ptr);
    
    float i = 34;
    float *ptr = &i;
    printf("THE ADDRESS OF i IS %u\n", ptr);
    ptr++;
    printf("THE ADDRESS OF i IS %u\n", ptr);
    return 0;
}
#include <stdio.h>

int main(){
    int principal=100, rate=12, time=12;
    int simple_interest = (principal*rate*time)/100;
    printf("THE VALUE OF SIMPLE INTERST IS %d",simple_interest);
    return 0;
}